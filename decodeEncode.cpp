#include <string>
#include <iostream>

using std::cout;
using std::cin;
using std::string;

//Takes input from cin and encodes to number from 0-63.
int inputFunc(void) {
  string input;
  int square;

  cin >> input;
  
  switch (input) {
    case "a1":

  }
  return 0;
}

string encode(int code){



  return "";
}

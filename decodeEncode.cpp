#include <string>
#include <iostream>

using std::cout;
using std::cin;
using std::string;

//Takes input from cin and encodes to number from 0-63.
int inputFunc(void) {
  string input;
  cin >> input;
  switch (input) {
    case :
  }
  return 0;
}

string encode(int code){



  return "";
}
